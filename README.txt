PrettyDate - Drupal Module

This module is meant for Drupal 7.x and creates a field formatter and a script that gets included on every page.

It makes use of John Resig's Prettydate formatter but is modified to fit into Drupal and so it works on dates older than 2 weeks old. 
